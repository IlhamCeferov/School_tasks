import math
from turtle import width

lenght = float(input("Enter the length of the rectangle: "))
widht = float(input("Enter the width of the rectangle: "))
perimeter = 2 * (lenght + widht)
print(f"The perimeter of the rectangle with length {lenght} and width {widht} is: {perimeter}")